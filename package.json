{
  "name": "herkules",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ready": "vp run -r build && vp check && vp run -r test && vp run test:deploy",
    "test:deploy": "python3 tools/ai/prepare-production.test.py && node --test tools/deploy/image-targets.verify.mjs tools/deploy/release.verify.mjs && sh tools/deploy/apply-release.test.sh && sh tools/deploy/backup/backup.test.sh",
    "test:caddy": "sh tools/deploy/caddy/entrypoint.test.sh",
    "prepare": "vp config",
    "build:edge": "vp run @herkules/web#build && vp -C apps/bbs/web build && node tools/deploy/cloudflare/prepare.mjs",
    "dev:edge:platform": "wrangler dev --config tools/deploy/cloudflare/platform.json --port 8787",
    "dev:edge:bbs": "wrangler dev --config tools/deploy/cloudflare/bbs.json --port 8788",
    "test:edge": "node tools/deploy/cloudflare/verify.mjs"
  },
  "devDependencies": {
    "vite": "catalog:",
    "vite-plus": "catalog:",
    "wrangler": "catalog:"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "11.24.0",
      "onFail": "download"
    },
    "runtime": {
      "name": "node",
      "version": "24.20.0",
      "onFail": "download"
    }
  },
  "engines": {
    "node": ">=22.18.0"
  }
}
