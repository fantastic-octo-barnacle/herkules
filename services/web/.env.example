# services/web has no runtime configuration: it is static and calls /auth on its own origin.
# For `vp run dev`, start services/auth and services/mcp-directory with PUBLIC_ORIGIN=http://localhost:3000;
# the dev server on :3000 proxies /auth, /.well-known and /mcp/directory to them.
