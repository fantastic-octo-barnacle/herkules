# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.wrangler
*.local

# dotenv environment variable files
.env
.env.*
!.env.example

# Terraform (tools/deploy/cloudflare/terraform). State can hold record contents
# and, at any later scope, secrets -- never commit it. The dependency lock file
# is deliberately NOT ignored: it pins provider versions and must be committed.
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
crash.*.log
*.tfvars
*.tfvars.json
!*.tfvars.example
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Editor directories and files
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# AI agent worktrees
.claude/worktrees/
.zcode/
!.vscode/
!.vscode/settings.json
!.vscode/extensions.json
