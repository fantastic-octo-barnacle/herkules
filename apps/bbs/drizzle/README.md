# apps/bbs/drizzle

Generated by `vp run db:generate` (drizzle-kit) from `src/db/schema.ts`. Never hand-edit a
generated `.sql` or `meta/` — herkules-old's snapshot went stale that way. The one manual step,
repeated after every regeneration of `0000_*.sql`: add `CREATE EXTENSION IF NOT EXISTS pg_trgm;`
as its first statement (drizzle-kit cannot emit it; the two `gin_trgm_ops` indexes need it). Tests
construct PGlite with `extensions: { pg_trgm }` for the same reason (`src/db/index.ts`).
